---

machine:
  python:
    version: 2.7.8

dependencies:
  pre:
    - pip install ansible
    - cp tests/ansible.cfg .

test:
  override:
    - ./tests/runtests.sh $CIRCLE_NODE_INDEX:
        parallel: true

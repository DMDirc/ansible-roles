{% for host in hosts %}
server {
        listen                          80;
        listen                          [::]:80;
        server_name                     {{ host }};
	{% for location in hosts[host] %}
	location = {{location.location}} {
		rewrite		^ {{location.redirect}} redirect;
	}
	{% endfor %}
	location ~* ^.* {
		rewrite		^ https://www.dmdirc.com redirect;
	}
}
{% endfor %}
